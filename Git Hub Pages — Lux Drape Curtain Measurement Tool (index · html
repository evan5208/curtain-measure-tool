<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Curtain Measurement Tool | LuxDrape</title>
  <meta name="description" content="Free, fast calculator to size custom curtains: width, panels, and finished length. Publishable on GitHub Pages with backlinks to LuxDrape." />
  <meta name="robots" content="index,follow" />
  <meta property="og:title" content="Curtain Measurement Tool | LuxDrape" />
  <meta property="og:description" content="Size custom curtains accurately: outside/inside mount, fullness, panels, finished length. One-page app you can host on GitHub Pages." />
  <meta property="og:type" content="website" />
  <style>
    body { font-family: Arial, sans-serif; margin: 0; background: #f6f8fb; color: #222; }
    header { background: #fff; border-bottom: 1px solid #ddd; padding: 20px; }
    header h1 { margin: 0; font-size: 24px; }
    header p { margin: 5px 0 0; font-size: 14px; color: #555; }
    .wrap { max-width: 1000px; margin: 0 auto; padding: 20px; }
    .card { background: #fff; border: 1px solid #ddd; border-radius: 8px; padding: 20px; margin-bottom: 20px; }
    label { display: block; margin-top: 12px; font-size: 14px; color: #333; }
    input, select { width: 100%; padding: 8px; margin-top: 5px; border: 1px solid #ccc; border-radius: 4px; }
    button { padding: 10px 16px; border: none; border-radius: 6px; cursor: pointer; margin-top: 12px; }
    .btn-primary { background: #007bff; color: #fff; }
    .btn-secondary { background: #f0f0f0; color: #222; }
    .results { margin-top: 20px; }
    .footer { text-align: center; font-size: 13px; color: #777; margin-top: 40px; }
    a { color: #007bff; text-decoration: none; }
    a:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <header>
    <div class="wrap">
      <h1>Curtain Measurement Tool</h1>
      <p>Accurate sizes for custom drapery — then visit <a href="https://luxdrape.com/" target="_blank" rel="noopener">LuxDrape</a> to order.</p>
    </div>
  </header>

  <main class="wrap">
    <div class="card">
      <h2>1) Enter your measurements</h2>
      <form id="form">
        <label for="width">Window width</label>
        <input id="width" type="number" placeholder="e.g. 72" required />

        <label for="height">Window height</label>
        <input id="height" type="number" placeholder="e.g. 48" />

        <label for="fullness">Fullness ×</label>
        <select id="fullness">
          <option value="1.8">1.8× (sleek)</option>
          <option value="2.0">2.0×</option>
          <option value="2.2" selected>2.2× (balanced)</option>
          <option value="2.5">2.5× (lush)</option>
        </select>

        <label for="panelWidth">Usable width per panel</label>
        <input id="panelWidth" type="number" placeholder="e.g. 50" />

        <button type="button" class="btn-primary" onclick="calculate()">Calculate</button>
        <button type="reset" class="btn-secondary">Reset</button>
      </form>
    </div>

    <div class="card">
      <h2>2) Results</h2>
      <div id="results" class="results">
        <p>Fill the form and press <strong>Calculate</strong>.</p>
      </div>
      <p><a href="https://luxdrape.com/" target="_blank" rel="noopener" class="btn-primary">Order at LuxDrape ↗</a></p>
    </div>

    <div class="card">
      <h2>3) Helpful links</h2>
      <ul>
        <li><a href="https://luxdrape.com/" target="_blank" rel="noopener">Custom Curtains — LuxDrape</a></li>
        <li><a href="https://luxdrape.com/collections/linen" target="_blank" rel="noopener">Linen Drapes Collection</a></li>
        <li><a href="https://luxdrape.com/collections/velvet" target="_blank" rel="noopener">Velvet Drapes Collection</a></li>
      </ul>
    </div>
  </main>

  <footer class="footer">
    © <span id="year"></span> LuxDrape. Natural backlinks to <a href="https://luxdrape.com/" target="_blank" rel="noopener">LuxDrape</a>.
  </footer>

  <script>
    function calculate() {
      const width = parseFloat(document.getElementById('width').value);
      const fullness = parseFloat(document.getElementById('fullness').value);
      const panelWidth = parseFloat(document.getElementById('panelWidth').value);

      if (!width || !fullness || !panelWidth) {
        document.getElementById('results').innerHTML = "<p>Please enter all values.</p>";
        return;
      }

      const fabricNeeded = width * fullness;
      const panels = Math.max(1, Math.ceil(fabricNeeded / panelWidth));
      const perPanel = (fabricNeeded / panels).toFixed(1);

      document.getElementById('results').innerHTML = `
        <p><strong>Rod width:</strong> ${width} in</p>
        <p><strong>Fullness:</strong> ${fullness}×</p>
        <p><strong>Total fabric needed:</strong> ${fabricNeeded.toFixed(1)} in</p>
        <p><strong>Panels required:</strong> ${panels}</p>
        <p><strong>Per panel finished width:</strong> ${perPanel} in</p>
      `;
    }
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
